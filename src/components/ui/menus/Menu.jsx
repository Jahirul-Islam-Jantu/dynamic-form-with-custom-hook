import React from 'react';
import {NavLink} from "react-router-dom";

const Menu = () => {
    return (
        <div>
            <NavLink to="/" >Dynamic Form</NavLink>
        </div>
    );
};

export default Menu;